# 29.7. Промежуточная сдача проекта (HW-03)

## Задание 29.1.1

В предыдущем модуле на странице index.js мы с вами сделали фильтр поиска по трансляциям. 
Очень вероятно, что нам придётся переиспользовать этот код где-то ещё,
поэтому полезно было бы оформить его в виде функции. 

> Оформите код из задания на фильтрацию контента страницы (юнит 28.5) в виде функции без параметров. 

При этом сделайте так, чтобы вызов функции у вас был на HTML-странице прямо из атрибута oninput,
а само тело функции лежало бы в отдельном файле.
